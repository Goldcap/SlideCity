# Claude API key for Direct Audience mayor chat (optional — game works without it)
ANTHROPIC_API_KEY=your_key_here

# Replicate API token for sprite generation (optional — colored rects work fine)
REPLICATE_API_TOKEN=your_token_here
